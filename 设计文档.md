好的，我已经把之前的 **智能个人仪表盘项目设计文档**整理成 **Markdown 格式**，方便你直接保存为 `.md` 文件或者在 Codex 中使用。

---

# 智能个人仪表盘项目设计文档（Vue 3 + TS 前后端分离）

## 一、项目概述

**项目名称**：智能个人仪表盘
**项目目标**：提供任务管理、日程安排、天气信息、数据可视化和 AI 助手功能，前端完全基于 Vue 3 + TypeScript，前后端分离，支持响应式布局和可部署于 GitHub Pages。

**技术栈**：

* 前端：Vue 3 + TypeScript + Vite + Pinia + Vue Router + Axios + Element Plus / Ant Design Vue
* 后端：Node.js / NestJS + Express + MongoDB/MySQL（RESTful API）
* AI接口：OpenAI API 或本地 LLM
* 数据可视化：ECharts
* 部署：前端 GitHub Pages，后端 Vercel/Render

---

## 二、项目架构

### 1. 页面结构

| 页面                       | 路由              | 功能描述                       | 核心组件                                                                            |
| ------------------------ | --------------- | -------------------------- | ------------------------------------------------------------------------------- |
| **仪表盘主页 (Dashboard)**    | `/dashboard`    | 显示今日任务、日程、天气和数据统计，提供概览     | `DashboardHeader`、`TaskSummary`、`CalendarOverview`、`WeatherWidget`、`StatsChart` |
| **任务管理 (Tasks)**         | `/tasks`        | 创建、编辑、删除任务；任务状态管理；AI 优先级建议 | `TaskList`、`TaskItem`、`TaskForm`、`AIPrioritySuggestion`                         |
| **日程管理 (Calendar)**      | `/calendar`     | 日历视图展示事件；事件创建、修改、删除；提醒通知   | `CalendarView`、`EventItem`、`EventForm`                                          |
| **AI 助手 (AI Assistant)** | `/ai-assistant` | AI 提供智能建议、笔记摘要、任务优化        | `AIInput`、`AISuggestions`、`AINotifications`                                     |
| **数据可视化 (Analytics)**    | `/analytics`    | 图表展示任务完成率、时间分布、效率统计        | `LineChart`、`BarChart`、`PieChart`                                               |
| **用户设置 (Settings)**      | `/settings`     | 主题切换、布局自定义、通知设置            | `ThemeToggle`、`LayoutSelector`、`NotificationSettings`                           |

---

### 2. 组件结构示例

```text
src/
│
├─ components/
│   ├─ Dashboard/
│   │   ├─ DashboardHeader.vue
│   │   ├─ TaskSummary.vue
│   │   ├─ CalendarOverview.vue
│   │   └─ StatsChart.vue
│   │
│   ├─ Tasks/
│   │   ├─ TaskList.vue
│   │   ├─ TaskItem.vue
│   │   ├─ TaskForm.vue
│   │   └─ AIPrioritySuggestion.vue
│   │
│   ├─ Calendar/
│   │   ├─ CalendarView.vue
│   │   ├─ EventItem.vue
│   │   └─ EventForm.vue
│   │
│   ├─ AI/
│   │   ├─ AIInput.vue
│   │   ├─ AISuggestions.vue
│   │   └─ AINotifications.vue
│   │
│   ├─ Analytics/
│   │   ├─ LineChart.vue
│   │   ├─ BarChart.vue
│   │   └─ PieChart.vue
│   │
│   └─ Settings/
│       ├─ ThemeToggle.vue
│       ├─ LayoutSelector.vue
│       └─ NotificationSettings.vue
│
├─ store/
│   ├─ taskStore.ts
│   ├─ calendarStore.ts
│   ├─ aiStore.ts
│   └─ userStore.ts
│
├─ router/
│   └─ index.ts
│
├─ api/
│   ├─ taskApi.ts
│   ├─ calendarApi.ts
│   └─ aiApi.ts
│
└─ views/
    ├─ DashboardView.vue
    ├─ TaskView.vue
    ├─ CalendarView.vue
    ├─ AIAssistantView.vue
    ├─ AnalyticsView.vue
    └─ SettingsView.vue
```

---

### 3. 路由配置示例 (`src/router/index.ts`)

```ts
import { createRouter, createWebHistory } from 'vue-router'
import DashboardView from '@/views/DashboardView.vue'
import TaskView from '@/views/TaskView.vue'
import CalendarView from '@/views/CalendarView.vue'
import AIAssistantView from '@/views/AIAssistantView.vue'
import AnalyticsView from '@/views/AnalyticsView.vue'
import SettingsView from '@/views/SettingsView.vue'

const routes = [
  { path: '/', redirect: '/dashboard' },
  { path: '/dashboard', component: DashboardView },
  { path: '/tasks', component: TaskView },
  { path: '/calendar', component: CalendarView },
  { path: '/ai-assistant', component: AIAssistantView },
  { path: '/analytics', component: AnalyticsView },
  { path: '/settings', component: SettingsView },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

export default router
```

---

### 4. Pinia 状态管理示例 (`src/store/taskStore.ts`)

```ts
import { defineStore } from 'pinia'

export interface Task {
  id: number
  title: string
  description?: string
  status: 'todo' | 'in-progress' | 'done'
  priority?: number
}

export const useTaskStore = defineStore('task', {
  state: () => ({
    tasks: [] as Task[],
  }),
  actions: {
    setTasks(tasks: Task[]) {
      this.tasks = tasks
    },
    addTask(task: Task) {
      this.tasks.push(task)
    },
    updateTask(task: Task) {
      const index = this.tasks.findIndex(t => t.id === task.id)
      if (index !== -1) this.tasks[index] = task
    },
    removeTask(id: number) {
      this.tasks = this.tasks.filter(t => t.id !== id)
    },
  },
})
```

---

### 5. API 封装示例 (`src/api/taskApi.ts`)

```ts
import axios from 'axios'
import type { Task } from '@/store/taskStore'

const api = axios.create({
  baseURL: 'https://your-backend-api.com/api', // 替换为你的后端地址
})

export const getTasks = async (): Promise<Task[]> => {
  const res = await api.get('/tasks')
  return res.data
}

export const createTask = async (task: Partial<Task>): Promise<Task> => {
  const res = await api.post('/tasks', task)
  return res.data
}

export const updateTask = async (task: Task): Promise<Task> => {
  const res = await api.put(`/tasks/${task.id}`, task)
  return res.data
}

export const deleteTask = async (id: number): Promise<void> => {
  await api.delete(`/tasks/${id}`)
}
```

---

### 6. 页面骨架示例 (`src/views/DashboardView.vue`)

```vue
<template>
  <div class="dashboard">
    <DashboardHeader />
    <div class="dashboard-widgets">
      <TaskSummary />
      <CalendarOverview />
      <StatsChart />
    </div>
  </div>
</template>

<script lang="ts" setup>
import DashboardHeader from '@/components/Dashboard/DashboardHeader.vue'
import TaskSummary from '@/components/Dashboard/TaskSummary.vue'
import CalendarOverview from '@/components/Dashboard/CalendarOverview.vue'
import StatsChart from '@/components/Dashboard/StatsChart.vue'
</script>

<style scoped>
.dashboard-widgets {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}
</style>
```

---

### 7. AI 功能调用示例 (`src/api/aiApi.ts`)

```ts
import axios from 'axios'

const api = axios.create({
  baseURL: 'https://your-ai-api.com/api', // 替换为你的 AI 后端
})

export const getAISuggestions = async (input: string): Promise<string[]> => {
  const res = await api.post('/ai/suggestions', { input })
  return res.data.suggestions
}
```

---

💡 **使用方法**：

1. 用 Codex 或其他 AI 工具补充组件内部逻辑（渲染列表、表单交互、AI 调用等）。
2. 修改 `api/*.ts` 地址为你自己的后端或 AI 服务。
3. 部署前端到 **GitHub Pages**，后端可以部署到 **Vercel / Render**。
4. 使用 Pinia 管理全局状态，Axios 调用 API，保证前后端分离。

---

如果你需要，我可以帮你进一步 **生成每个页面和组件的完整 Vue skeleton，包括 Props、Emit、基础事件处理和 UI 占位**，这样几乎可以“一键启动项目开发”。

你希望我帮你生成这个完整 skeleton 吗？
