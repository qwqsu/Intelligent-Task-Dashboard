# 智慧任务仪表盘｜简历项目与面试准备

## 一、简历项目内容

### 智慧任务仪表盘　　　　　　　　　　　　　　　　　　　　　　　　　2026.03

GitHub：https://github.com/qwqsu/Intelligent-Task-Dashboard

**技术栈：** Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus、ECharts、Axios、Day.js、Node.js、Express、RESTful API、DeepSeek API

**项目简介：**

基于 Vue 3 + TypeScript 前后端分离架构开发的个人效率管理平台，以“今日工作台”为核心整合任务、日程、复盘和 AI 助手。前端采用组件化和响应式布局，后端使用 Express 提供任务/日程 RESTful API，并通过服务端代理 DeepSeek 请求，支持自然语言创建任务与日程。

**项目亮点：**

1. **搭建可配置的工作区框架。** 使用 Vue Router 划分总览、任务、日程、复盘、AI 助手和设置页面，路由组件采用动态导入按需加载；通过 CSS 变量和 Pinia 实现亮色/暗色主题、标准/紧凑布局及移动端底部导航，统一处理侧栏、卡片、按钮和图表的视觉状态。

2. **完善任务管理和状态流转。** 基于 Pinia 建立任务状态模型，支持新增、编辑、删除、状态切换、关键词搜索、优先级筛选、按优先级/截止时间/创建时间排序，以及“今天/逾期”范围筛选；任务完成时记录 `completedAt`，为后续复盘统计提供可靠时间依据。

3. **实现前后端同步与离线兜底。** 使用 Axios 封装任务和日程 API，后端通过 Express 提供 `GET/POST/PATCH/DELETE` 接口；结合 `pinia-plugin-persistedstate` 持久化任务、日程和用户设置，接口暂时不可用时保留本地数据，并在新增、修改、删除操作中提供本地回退逻辑，避免页面刷新后状态丢失。

4. **处理日历和时间边界问题。** 使用 Day.js 统一本地日期格式化、今天/逾期判断、事件排序和完成趋势计算；修复 UTC 偏移导致的跨日显示问题，并在前端表单校验和 Store 数据归一化阶段保证事件结束时间晚于开始时间，兼容历史非法日程数据。

5. **构建上下文感知的 AI 助手。** 前端将当前任务列表、日程列表和对话历史发送到后端，服务端在 system prompt 中注入当前日期和业务规则，调用 DeepSeek API 生成回复；通过 `create_task/create_event` 结构化操作块解析 AI 意图，前端二次校验标题、优先级和时间关系后再调用对应 Store，避免模型输出直接修改数据。

6. **使用 ECharts 构建复盘视图。** 以环形图、柱状图和折线图展示任务状态分布、优先级分布、完成率和近期完成趋势；趋势统计优先使用 `completedAt`，没有完成时间时才回退到 `createdAt`，使图表数据和实际完成行为保持一致。

7. **完善异常处理和开发体验。** 修复 `localhost` 与 `127.0.0.1` 开发地址之间的 CORS 问题、表单校验 rejected Promise 未捕获问题、Vite 重复 `base` 配置问题；外部天气接口失败时提供离线提示和重试入口，并为 AI 服务不可用保留友好的对话反馈。

---

## 二、面试官可能提问与参考回答

以下回答建议根据自己的实际理解进行调整，不建议逐字背诵。

### 1. 请介绍一下这个项目的整体架构。

**参考回答：**

项目采用前后端分离架构。前端是 Vue 3 + TypeScript + Vite，通过 Vue Router 管理页面，通过 Pinia 管理任务、日程、用户和 AI 对话状态，Axios 负责请求后端 API。后端是 Node.js + Express，提供任务和日程的 RESTful CRUD 接口，同时代理 DeepSeek API。开发阶段使用 JSON 文件保存数据，前端另外保留 Pinia 持久化数据作为接口异常时的本地兜底。

### 2. 为什么使用 Pinia，而不是只在组件中维护状态？

**参考回答：**

任务和日程会被总览、任务页、日历页、复盘页和 AI 助手共同使用，如果把状态放在组件内部，就需要通过多层 props 和事件传递。Pinia 可以集中维护状态、派生数据和业务操作，例如 `todayTasks`、`overdueTasks`、`todayCompletion` 等 getter，页面只负责展示和触发行为，业务逻辑更容易复用和测试。

### 3. 项目如何解决刷新后数据丢失的问题？

**参考回答：**

我使用 `pinia-plugin-persistedstate` 对任务、日程和用户设置进行持久化。正常情况下，登录后 Store 会从后端加载最新数据；如果请求失败，Store 的 catch 分支不会清空已有状态，而是继续使用本地持久化数据。新增、修改和删除也采用“优先请求后端，失败时更新本地”的策略，所以开发演示时后端短暂不可用不会让页面直接变成空白。

### 4. 任务页的搜索、筛选和排序是怎么组织的？

**参考回答：**

原始任务数据只保存在 `taskStore.tasks` 中，TaskList 使用 computed 生成展示列表。它依次应用状态 Tab、优先级、今天/逾期范围和关键词条件，最后再根据优先级、截止时间或创建时间排序。这样筛选条件变化时 Vue 会自动重新计算，不需要手动维护多份列表，也避免了不同页面出现状态不一致。

### 5. 项目中日期处理遇到过什么问题？如何解决？

**参考回答：**

最容易出问题的是把只有日期的字符串交给 `Date` 或按 UTC 计算，用户在不同地区可能会看到任务提前或延后一天。项目中使用 Day.js，并用 `YYYY-MM-DD` 字符串完成“今天”和事件归属判断；涉及时间先转成 Day.js 对象再比较。对于事件，还会检查结束时间是否晚于开始时间，并对后端历史数据中结束时间早于开始时间的记录做归一化。

### 6. 日历模块为什么不直接使用一个完整的日历组件？

**参考回答：**

这个项目的日历交互比较简单但有定制需求，例如日期单元格显示事件数量和颜色点、双击日期快速创建、选中日期后在右侧展示事件。因此我实现了一个轻量月历组件，用 Day.js 计算月初、月末和补齐的前后月份日期，再通过 `eventsOnDate` getter 映射当天事件。这样结构更可控，也避免引入一个功能过重的日历库。

### 7. AI 助手是如何做到“创建任务”的？

**参考回答：**

前端发送对话历史和当前任务、日程上下文，后端把当前日期、可用操作和字段格式写入 system prompt。AI 如果识别到创建任务，会在普通回复后返回一个 `action` 代码块，例如 `create_task` 和对应 payload。服务端先提取并解析 JSON，前端再检查标题、优先级和日期字段，校验通过后调用 `taskStore.addTask`。AI 只负责理解意图，真正的数据修改仍然由 Store 和 API 完成。

### 8. 为什么 AI 请求必须放在后端？

**参考回答：**

DeepSeek API Key 不能放在前端构建产物里，否则用户可以直接查看并盗用。因此前端只调用自己的 `/api/ai/messages` 等接口，后端使用环境变量读取 Key，再通过 OpenAI 兼容 SDK 请求 DeepSeek。后端也统一处理 system prompt、当前日期和上下文，方便控制模型行为。

### 9. 如何防止 AI 输出错误数据？

**参考回答：**

我没有把模型返回的内容直接当成操作执行，而是约定结构化 `action` 块，并在前端做二次校验：任务必须有非空标题，优先级必须是 `low/medium/high`；日程必须有标题、开始时间和结束时间，并且结束时间晚于开始时间。解析失败或字段不完整时只保留文字回复，不执行数据操作。生产环境还可以进一步使用 Zod 或 JSON Schema 在服务端做严格校验。

### 10. 项目中的 RESTful API 是如何设计的？

**参考回答：**

任务和日程分别使用资源路径 `/api/tasks` 和 `/api/events`。获取列表使用 `GET`，创建使用 `POST`，修改使用 `PATCH /:id`，删除使用 `DELETE /:id`；成功创建返回 201，资源不存在返回 404，删除成功返回 204。前端通过 Axios 模块封装这些请求，Store 不直接拼接 URL，降低了页面和接口实现之间的耦合。

### 11. 为什么用 JSON 文件，而不是数据库？有什么局限？

**参考回答：**

这个项目定位是个人效率工具和简历演示，JSON 文件部署成本低、结构直观，也方便快速展示 CRUD 流程。但它不适合多用户、高并发和复杂查询，多个请求同时写入还可能产生覆盖问题。如果继续产品化，我会替换为 PostgreSQL/MySQL，增加用户表、任务表和日程表，通过用户 ID 隔离数据，并加入事务、分页和索引。

### 12. ECharts 页面统计了哪些内容？为什么使用 `completedAt`？

**参考回答：**

复盘页展示任务总数、完成率、逾期数量、本周完成数，以及状态、优先级和近几天完成趋势。任务创建时间只能说明任务什么时候进入系统，不能代表什么时候完成，因此我在状态切换为 done 时记录 `completedAt`，趋势优先按完成时间统计；老数据没有该字段时才回退到创建时间。

### 13. 主题切换是怎么实现的？

**参考回答：**

主题状态由 Pinia 的 `userStore.theme` 管理，切换时给 `document.documentElement` 添加或移除 `dark` class，所有颜色通过 CSS 变量引用，所以不需要刷新页面。暗色主题中我为页面、内容表面、边框、文字和侧栏分别定义变量。之前出现过暗色侧栏变白的问题，原因是侧栏背景误用了同时承担文字色语义的 `--ink-800`，后来改成独立的 sidebar token，避免主题变量互相污染。

### 14. 项目如何适配移动端？

**参考回答：**

桌面端采用侧栏导航，屏幕宽度较小时隐藏侧栏并显示底部导航；仪表盘、任务工具栏、日历和图表使用响应式 Grid/Flex 布局自动换列。移动端还调整内容间距、按钮排列和日历单元格高度，并实际用窄视口检查了页面没有横向溢出。

### 15. 项目做了哪些性能方面的处理？

**参考回答：**

页面路由使用动态导入，使登录、任务、日历、复盘和 AI 页面可以按路由拆分加载；图表和复杂页面不会全部在首屏初始化。组件内部使用 computed 派生筛选列表，避免无关状态变化时重复手动处理数据。后续还可以继续拆分 Element Plus 和 ECharts vendor chunk，并根据访问数据决定是否按页面异步加载图表库。

### 16. 项目中最有代表性的 Bug 是什么？你是怎么定位的？

**参考回答：**

一个比较典型的问题是使用 `http://127.0.0.1:5173` 打开前端时，登录成功但任务列表为空。通过浏览器 Network 和后端响应检查发现，后端 CORS 只允许 `localhost`，没有允许 `127.0.0.1`。我补充了两种开发地址的白名单，并重新验证了前端数据加载。另一个主题问题则是通过检查暗色模式下的 computed style，定位到侧栏背景错误引用浅色文字变量。

### 17. 当前登录实现是否适合生产环境？

**参考回答：**

目前登录是前端本地演示账号，用户信息由 Pinia 持久化，不具备生产级安全性。这样做是为了让项目无需额外用户服务也能演示完整流程。若用于生产，我会把登录移到后端，使用 bcrypt/Argon2 哈希密码、Session 或 JWT 管理会话，接口增加认证中间件，并按用户 ID 查询任务和日程，绝不会把明文密码和用户数据保存在前端。

### 18. 如果继续迭代，你会优先做什么？

**参考回答：**

我会优先做三件事：第一，把 JSON 存储迁移到数据库并补充用户隔离；第二，为接口和 AI action 增加统一的 schema 校验、错误码和请求日志；第三，补充 Store、日期计算和关键交互的自动化测试。功能层面可以继续增加重复日程、拖拽排期、任务标签和更细的生产力分析。

---

## 三、面试时需要主动说明的边界

- 当前后端使用 JSON 文件，适合个人项目演示，不应表述为高并发生产存储方案。
- 当前登录是前端本地演示账号，不应表述为完整的用户认证与权限系统。
- AI 输出采用“结构化 action + 前端校验”，不是让模型直接操作数据库。
- 天气来自外部接口，接口失败时展示离线提示，不应表述为自建天气服务。
- 项目没有虚构性能提升百分比；如果面试官询问量化指标，应说明目前主要完成功能和工程质量验证，后续可通过埋点和压测补充数据。
